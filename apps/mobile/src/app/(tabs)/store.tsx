/**
 * 할인 쿠폰 탭 — "내 혜택"과 "제휴 혜택"을 하나로 합쳤다 (2026-09-08).
 * 무료 오픈 이후 모두에게 열려 있으므로 화면은 lib/coupons 하나면 된다.
 * 홈 카테고리 타일은 ?cat= 파라미터로 이 탭의 해당 카테고리를 바로 연다.
 */
export { default } from '../../lib/coupons';
